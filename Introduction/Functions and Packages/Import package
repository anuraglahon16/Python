# Definition of radius
r = 0.43

# Import the math package

import math as m
# Calculate C
C = 2*m.pi*r

# Calculate A
A = m.pi*r**2

# Build printout
print("Circumference: " + str(C))
print("Area: " + str(A))
